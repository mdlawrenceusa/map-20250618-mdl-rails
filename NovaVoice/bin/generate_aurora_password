#!/bin/bash

# Generate Aurora DSQL auth token for Rails
hostname="lyabugbxayepatoxdewlxwdo7q.dsql.us-east-1.on.aws"
region="us-east-1"

aws dsql generate-db-connect-admin-auth-token --hostname "$hostname" --region "$region"